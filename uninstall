#!/bin/bash


path=$(rpm -qa gridwiz)

if [ -z $path ]
then
  echo -e "The Package does not exist."
  exit

fi

while true; do
read -p "Uninstall Package [y/n] " ans

case $ans in
  [Yy]* ) rpm -e $path; echo "The Pacakge was removed"; break;;
  [Nn]* ) exit;;
  * ) echo -e "Press answer y or n";; 
  esac
done

